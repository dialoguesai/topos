"""Engine-coupled half of the topos-eval suite — implements the topos_eval protocols against
a real Topos node. Imports FROM topos_eval (protocols) and FROM the engine; topos_eval must
never import this. See README.md for the seam."""
