"""§G — dense-artifact sparsification evals.

The Dense Intelligence Upgrade adds condensed artifacts (stat rollups, entity dossiers, atomic
facts) that are higher-stakes leaks — one line can fingerprint a person. These evals prove each
gets *sparser* for a grantee: owner-only by default, exposed only when the scope explicitly
grants the artifact type, and PII-scrubbed even then.
"""
