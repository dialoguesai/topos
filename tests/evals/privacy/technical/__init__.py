"""§F.8 technical evals: regressions that would break a privacy claim without being a claim.

Not marketing metrics — invariants (idempotence, monotonicity, merge-restrictiveness,
fail-closed) whose violation silently degrades enforcement.
"""
